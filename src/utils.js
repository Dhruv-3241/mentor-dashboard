export const studentUrl = (endpoint) =>  `https://mentor-dash.onrender.com/api/students/${endpoint}`;
export const mentorUrl = (endpoint) =>  `https://mentor-dash.onrender.com/api/mentors/${endpoint}`;

// export const studentUrl = (endpoint) => `http://localhost:4000/api/students/${endpoint}`;
// export const mentorUrl = (endpoint) => `http://localhost:4000/api/mentors/${endpoint}`;